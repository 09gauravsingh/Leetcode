class Solution:
    def numIdenticalPairs(self, nums: List[int]) -> int:
        summ = 0
        for i in range(len(nums)):
            for j in range(i+1,len(nums)):
                if nums[i] == nums[j]:
                    summ += 1
             
        return summ   
                
        
