class Solution:
    def subtractProductAndSum(self, n: int) -> int:
        summ = 0
        mult = 1
        for i in str(n):
            mult = mult*int(i)
            summ += int(i)
            result = mult - summ
            
        return result   
